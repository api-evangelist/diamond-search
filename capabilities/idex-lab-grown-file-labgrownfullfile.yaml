naftiko: 1.0.0-alpha2
info:
  label: Diamond Search Lab Grown Diamond Feed API — Labgrownfullfile
  description: 'Diamond Search Lab Grown Diamond Feed API — Labgrownfullfile. 1 operations. Lead operation: Diamond Search
    Download Complete Lab Grown File. Self-contained Naftiko capability covering one Diamond Search business surface.'
  tags:
  - Diamond Search
  - Labgrownfullfile
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    DIAMOND_SEARCH_API_KEY: DIAMOND_SEARCH_API_KEY
capability:
  consumes:
  - type: http
    namespace: idex-lab-grown-file-labgrownfullfile
    baseUri: https://api.idexonline.com/onsite/api
    description: Diamond Search Lab Grown Diamond Feed API — Labgrownfullfile business capability. Self-contained, no shared
      references.
    resources:
    - name: labgrownfullfile
      path: /labgrownfullfile
      operations:
      - name: post
        method: POST
        description: Diamond Search Download Complete Lab Grown File
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: apikey
      key: Authorization
      value: '{{env.DIAMOND_SEARCH_API_KEY}}'
      placement: header
  exposes:
  - type: rest
    namespace: idex-lab-grown-file-labgrownfullfile-rest
    port: 8080
    description: REST adapter for Diamond Search Lab Grown Diamond Feed API — Labgrownfullfile. One Spectral-compliant resource
      per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/labgrownfullfile
      name: labgrownfullfile
      description: REST surface for labgrownfullfile.
      operations:
      - method: POST
        name: post
        description: Diamond Search Download Complete Lab Grown File
        call: idex-lab-grown-file-labgrownfullfile.post
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: idex-lab-grown-file-labgrownfullfile-mcp
    port: 9090
    transport: http
    description: MCP adapter for Diamond Search Lab Grown Diamond Feed API — Labgrownfullfile. One tool per consumed operation,
      routed inline through this capability's consumes block.
    tools:
    - name: diamond-search-download-complete-lab
      description: Diamond Search Download Complete Lab Grown File
      hints:
        readOnly: true
        destructive: false
        idempotent: false
      call: idex-lab-grown-file-labgrownfullfile.post
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
